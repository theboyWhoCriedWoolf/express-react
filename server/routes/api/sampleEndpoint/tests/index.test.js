/**
 *
 * Test sampleApi
 *
 */
// import request from 'supertest';
// import express from 'express';

// import sampleApi from '..';

// const app = express().use(sampleApi(express.Router()));

describe('/sampleApi', () => {
  //   it('should return text/html', done => {
  //     request(app)
  //       .get('/')
  //       .expect('Content-Type', 'text/html; charset=utf-8')
  //       .expect(200, done);
  //   });
  //   it('should error with the wrong data type', done => {
  //     request(app)
  //       .post('/my/route')
  //       .expect(404, done);
  //   });
  //   it('should return "Hello World" in response', done => {
  //     request(app)
  //       .get('/')
  //       .query({ value: 'Hello World' })
  //       .expect('[ Making GET request to sampleApi API with "Hello World" ]')
  //       .expect(200, done);
  //   });
});
